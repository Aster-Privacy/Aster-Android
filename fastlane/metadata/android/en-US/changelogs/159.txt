Bug fixes and improvements:

- The F-Droid build now leaves out on-device email translation, so F-Droid can build the whole app from source. Translation stays available in the app you get from Google Play and from astermail.org
